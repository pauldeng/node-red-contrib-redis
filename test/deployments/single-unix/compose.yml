services:
  redis:
    image: ${REDIS_STANDALONE_IMAGE:-redis:latest}
    command:
      - redis-server
      - /usr/local/etc/redis/redis.conf
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf:ro
      - ${REDIS_UNIX_SOCKET_DIR:-/tmp}:/run/redis-socket
