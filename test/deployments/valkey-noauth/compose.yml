services:
  redis:
    image: ${VALKEY_STANDALONE_IMAGE:-valkey/valkey:latest}
    command:
      - redis-server
      - --bind
      - 0.0.0.0
      - --protected-mode
      - "no"
      - --save
      - ""
      - --appendonly
      - "no"
    ports:
      - "127.0.0.1:6379:6379"
