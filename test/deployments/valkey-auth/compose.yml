services:
  redis:
    image: ${VALKEY_STANDALONE_IMAGE:-valkey/valkey:latest}
    command:
      - redis-server
      - /usr/local/etc/redis/redis.conf
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - ./redis.conf:/usr/local/etc/redis/redis.conf:ro
